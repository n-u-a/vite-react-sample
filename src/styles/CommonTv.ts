import { tv } from "tailwind-variants";

export const header1 = tv({
  base: "text-xl border-b font-bold text-left mb-2",
});

export const header2 = tv({
  base: "text-l border-b font-bold text-left mb-2",
});
